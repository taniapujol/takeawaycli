export class Card {
    constructor (
        id: number,
        nombre: string,
        foto: string,
        descripcion: string,
        precio: number,
        activado: string,
        id_categoria: number
    ){}
}